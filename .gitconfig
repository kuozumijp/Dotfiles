[color]
	status = auto
	diff = auto
	branch = auto
	interactive = auto
	grep = auto
[alias]
	co = checkout
	ci = commit -a
	st = status
	up = pull --rebase
	br = branch
	si = submodule init
	su = submodule update
	cl = clone --recursive
[github]
	user = user
