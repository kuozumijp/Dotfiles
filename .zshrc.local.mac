#
# kuozumi .zshrc.local for mac
#

export PATH=$PATH:/usr/X11R6/bin:/etc:$HOME/bin

# aliaes
zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z} r:|[-_.]=**'
compdef mosh=ssh

# boxen
source /opt/boxen/env.sh
